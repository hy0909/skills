---
name: human-writing
description: 한국어 글을 사람이 쓴 것처럼, AI 티 없이, 개발·디자인을 모르는 사람도 바로 이해하게 쓰는 공통 문체 규칙. 두 가지로 쓴다. (A) 작성 모드 — 사용자가 쓰는 모든 한국어 산문(기획서·정책·이슈·PR·답변·플로우 노트·블로그)에 항상 적용. (B) 윤문 모드 — 사용자가 "윤문해줘", "AI 티 없애줘", "번역투 제거", "사람이 쓴 것처럼 다듬어", "자연스럽게 고쳐"라고 하면 진단→수정→변경량 보고 절차로 실행. 다른 스킬(feature-generator, component-generator, issue-generator, pr-generator, spec-sync, uxflow-generator, prototype-generator)이 글을 만들 때도 이 규칙이 기본값이다.
---

# Human Writing — 한국어 글 공통 문체 규칙

목표는 세 가지다. 사람이 쓴 것처럼 읽히기. 개발·디자인을 모르는 사람도 한 번에 이해하기. 핵심만 짧게 전달하기.
한국어 AI 글은 영어 글과 티 나는 지점이 다르다. **번역투가 첫 신호**고, 그다음이 `~습니다`만 반복되는 어미, `첫째·둘째·셋째` 나열, `결론적으로`다. 아래 규칙은 그 순서대로 막는다.

## 0. 적용 범위와 예외

- 적용: 문단으로 쓰는 모든 한국어 글. 기획서·정책 문서의 설명 문단, 이슈·PR 본문, 채팅 답변, 플로우 note, 블로그·제안서.
- 예외 1 — **표 셀, 노드 라벨, 제목, 요구사항 행**은 각 스킬의 명사형 종결 규칙(`데이터 출처 정의`, `주문 폼 진입`)을 그대로 지킨다. 어미 변주·도치·문장 길이 변주는 산문에만 쓴다.
- 예외 2 — **사실 문서(명세·정책·이슈·PR)에는 "관점" 규칙(§5)을 적용하지 않는다.** 반론·[내 경험] 자리는 의견 글(블로그·제안·회고·답변)에만 넣는다.
- 예외 3 — 사용자가 "그대로 두라"고 한 원문, 인용문, 코드, 용어 정의는 손대지 않는다.

## 1. 문장 — 결론 먼저, 접속사 대신 배치

- **문단의 첫 문장은 그 문단의 결론이나 행동이다.** 이유와 배경은 뒤에 온다.
  - 예: `기존 컴포넌트가 있어도 그대로 쓰기는 어려웠다. 제품마다 화면 구성과 사용 흐름이 달랐고, 무엇을 그대로 쓰고 무엇을 바꿔도 되는지 정해져 있지 않았다.`
- 한 문장에 생각 하나, 60자 안팎. 생각이 둘일 때만 나눈다. 대조·원인·조건·나열은 `~지만`, `~고`, `~기 때문에`, `~서`, `~면`으로 한 문장에 잇는다. 60자를 조금 넘어도 논리가 하나면 쪼개지 않는다.
- **논리 관계는 접속사보다 문장 배치로 보여 준다.** 앞 문장이 원인이면 다음 문장은 접속사 없이 결과를 쓴다. `그 결과`, `이러한`, `이를 통해`, `따라서`는 대부분 지워도 읽힌다. `다만`, `이렇게`, `그래서`는 한 문단에 한 번까지.
- 짧은 문장 뒤에 문두 `하지만/그래서/그러나`로 시작하는 짧은 문장을 붙이는 "툭툭 끊기"는 AI 리듬이다.
  - 나쁨: `기존 컴포넌트는 있었다. 하지만 제품마다 화면 구성이 달라 그대로 쓰기 어려웠다.`
  - 좋음: `기존 컴포넌트는 있었지만, 제품마다 화면 구성과 사용 흐름이 달랐기 때문에 그대로 가져다 쓰기 어려웠다.`
- **문단을 목적·이유 문장으로 닫는 것은 자연스럽다.** `~기 위해서다`, `~려는 것이다`, `~할 수 있도록 했다`는 그대로 쓴다. 금지하는 것은 앞 내용을 되풀이하는 요약 문장이다.
- 문장 길이는 20~60자 사이에서 저절로 섞인다. 억지로 도치하거나 조각 문장을 넣지 않는다.

## 2. 쉬운 말 — 풀어 쓰고, 일상 동사로

- **한자어 명사구는 `~ㄴ지` 절이나 동사 문장으로 푼다.**
  - `공통으로 유지할 부분과 바꿀 부분의 기준이 없었다` → `무엇을 그대로 쓰고 무엇을 바꿔도 되는지 정해져 있지 않았다`
  - `각 요소의 사용 조건과 변경 범위를 명시한다` → `언제 쓰는지, 어디까지 바꿀 수 있는지 적어 둔다`
- **일상 동사를 고른다.** 아래 왼쪽이 보이면 오른쪽으로.

| 문서 말 | 일상 말 |
| --- | --- |
| 활용하다 | 쓰다 |
| 명시하다, 기록하다 | 적다, 적어 두다 |
| 변경하다 | 바꾸다 |
| 수정하다 | 고치다, 손보다 |
| 파악하다 | 살피다, 보다 |
| 구분하다 | 나누다, 나눠 두다 |
| 재사용하다 | 이어 쓰다 |
| 선별하다 | 추리다 |
| 논의하다(반복) | 다시 이야기하다 |

- **전문용어는 우리말이 있으면 바꾼다.** `베리언트` → `형태별 구분`, `프로퍼티` → `속성`. 팀이 매일 쓰는 말(피그마, 토큰, 컴포넌트, 레이어, JSON, Markdown)은 그대로 두고 설명하지 않는다. 독자가 팀 밖일 때만 첫 등장 **다음 문장**에서 짧게 설명한다.
- **서술 문장 한가운데 괄호 설명을 끼우지 않는다.** `기존 컴포넌트(버튼·입력창처럼 화면을 이루는 부품)는 있었다`는 흐름을 끊는다.
- **같은 동사가 반복돼도 자연스러우면 둔다.** `줄인다`가 문단마다 나와도 된다. 억지 동의어(`수고가 준다`, `수정을 막는다`)로 바꾸지 않는다. 반복이 거슬리면 단어가 아니라 문장 구조를 바꾼다.
- **소제목은 짧은 `~한다` 문장, 일상어로.** `같은 일을 다시 하는 시간을 줄인다`, `디자인과 코드가 같은 대상을 같은 이름으로 부르게 한다`. 한자어 명사 소제목(`반복 작업 감소`)은 피한다.
- 예시는 독자의 일상에서 가져온다. 개발자만 아는 비유는 쓰지 않는다.

## 3. 금지 표현 — 한국어 AI 티

상세 목록과 고친 예시는 `references/ai-patterns-ko.md`. 아래는 매번 확인하는 최소 목록이다.

| 분류 | 금지 | 대신 |
| --- | --- | --- |
| 번역투 | `~를 통해`, `~에 있어서`, `~에 의해`, `~되어지다`, `위치해 있다`, `~함에도 불구하고`, `~를 가지고 있다`, `~에 대해` 남용 | `~로`, `~에서`, `~가`, `~되다`, `~에 있다`, `~지만`, `~가 있다` |
| 상투 관용구 | `결론적으로`, `시사하는 바가 크다`, `주목할 만하다`, `~하는 것이 중요합니다`, `~할 필요가 있다`, `이유는 단순하다`, `핵심은 다음과 같다`, `중요한 점은` | 문장 삭제. 또는 왜 중요한지 구체 상황으로 서술 |
| 기계적 단어 | `파고들어 봅시다`, `잠재력을 발휘하다`, `혁신적인`, `획기적인`, `원활한`, `강력한`, `다양한`, `효과적으로`, `성공적으로`, `~의 지평/여정/핵심 요소` | 구체 동작·상황 |
| 대조 구문 | `단순한 ~가 아니라 ~다`, `~뿐만 아니라 ~도/이기도 하다` | 주장 하나만 쓴다 |
| 구조 | `첫째·둘째·셋째` 나열, 형용사·명사 3개 나열, 문두 `또한/따라서/아울러/즉/이와 더불어/그 결과/이러한` 연속, 문단 안 볼드 라벨 나열(`**확장성:** ~`), 대시(—), 앞 내용을 되풀이하는 문단 끝 요약, 수사적 질문 뒤 즉답 | 문장을 이어 쓰기. 필요하면 표. 문서 소제목과 목적·이유로 닫는 문장은 예외 |
| 끊기 | 짧은 문장 + 문두 `하지만/그래서/그러나` 짧은 문장 연속. 대조·원인을 두 문장으로 쪼갠 것 | `~지만`, `~기 때문에`로 한 문장에 잇기 |
| 어미 | `~습니다`가 세 문장 연속 | 하나는 `~다`, `~요`, 명사형, 질문형으로 바꾸기 |
| 접사 | `-적`, `-성`, `-화` 남발 | 절반으로 줄이기 |
| 강조 | `매우`, `정말`, `굉장히` 자동 삽입, 동의어 이중 수식 | 삭제 |
| 완곡 | `~할 수 있을 것으로 보인다` 같은 다중 완곡 | 한 번만. `~할 수 있도록 한다`, `~려 한다`는 정상 표현이라 허용 |
| 형식명사 | `~것이다`, `~점`, `~할 필요가 있다` 남발 | 동사로 끝내기. 문단 끝 `~려는 것이다`는 허용 |
| 억지 수정 | 윤문하면서 넣은 억지 동의어, 서술 중 괄호 설명, 조각 문장, 도치 | 원문 표현으로 되돌리기 |
| 잔여물 | `좋은 질문입니다`, `도움이 되었으면 합니다`, `요약하면` 마무리 | 삭제 |

## 4. 구체성 — 막연한 말은 상황으로 그린다

- 막연한 말은 **독자가 그려 볼 수 있는 상황**으로 바꾼다. 숫자가 있으면 숫자, 없으면 행동과 상황.
  - `합의된 기준을 바탕으로 작업할 수 있게` → `다음 제품을 만들 때는 이미 정한 내용을 다시 논의하지 않고 시작할 수 있게`
  - `AI 기술은 기업 경영에서 매우 중요한 역할을 한다` → `AI가 고객 데이터로 소비 패턴을 예측하면, 상품 기획과 마케팅을 더 구체적으로 짤 수 있다`
- `많은`, `다양한`, `최근`, `크게 개선`은 숫자·기간·이름으로 바꾼다. **실제 정보가 없으면 지어내지 않고** `[확인 필요]`를 남긴다. 명세·정책에서는 각 스킬의 `TBD` / `원문 기준 추가 정의 필요`를 쓴다. 원문이 이미 구체적인 상황을 말하고 있으면 숫자를 요구하지 않는다.
- `중요하다`, `필요하다`, `시사한다`는 지운다. 대신 **왜** 중요한지, **어떤 상황**에서 의미가 있는지 한 문장으로 쓴다.

## 5. 관점 — 의견 글에만

- 장점만 쓰지 않는다. 아쉬운 점이나 "아직 모르겠다"는 판단을 한 군데 넣는다.
- 글 중간에 회의적인 독자가 낼 반론 하나를 넣고 정직하게 답한다. 일부 맞으면 인정한다.
- 작성자의 해석이 드러나는 문장을 한두 개 넣는다. `이 사례를 나는 ~로 이해했다` 식으로.
- 개인 경험이 들어갈 자리는 지어내지 말고 `[내 경험]`으로 비워 둔다.
- `references/voice-samples.md`의 기준 글과 "내가 직접 고친 문장"을 먼저 읽는다. 그 문장 길이·동사 선택·문단 여닫는 방식을 따른다. 문장이나 주제를 그대로 재사용하지는 않는다.

## 6. 표 vs 줄글

- **비교·대조가 가능한 항목은 표로 쓴다.** 항목이 2개 이상이고 같은 속성(조건·값·담당·상태)을 비교할 수 있으면 표다.
- 순서가 있는 절차는 번호 목록. 하나의 논리를 이어 설명할 때는 줄글.
- 표로 할지 줄글로 할지 판단이 서지 않으면 **사용자에게 묻는다** (Claude Code에서는 AskUserQuestion 도구). 질문 형식: "이 부분은 표로 정리할까요, 줄글로 쓸까요?" 선택지 2개(표 / 줄글)에 각 형식의 장점을 한 줄씩 붙인다. 답을 기다리는 동안 다른 부분을 먼저 쓴다.
- 표 셀은 짧게. 셀 안에 문장 3개가 넘으면 표가 아니라 줄글이 맞다.

## 6-1. 구조 — 소제목과 표 묶음 (human-writing을 직접 적용하는 글에만)

새 글을 쓰기 전에 정한다. 다른 스킬(feature-generator, issue-generator 등)이 정해 둔 문서 형식이 있으면 그 형식을 따르고 이 절은 건너뛴다.

- **소제목을 쓸지 먼저 사용자에게 묻는다** (Claude Code에서는 AskUserQuestion). 질문 형식: "이 글에 소제목을 넣을까요, 없이 이어 쓸까요?" 선택지 2개 — `소제목 있음`(상황별로 나눠 훑어 읽기 좋음) / `소제목 없음`(짧은 글·채팅 답변·한 흐름 설명에 맞음). 답을 기다리는 동안 본문을 먼저 쓴다. 이미 소제목 여부가 정해진 요청(형식을 지정했거나 윤문)이면 묻지 않는다.
- 소제목을 쓴다면 아래를 지킨다.

| 규칙 | 나쁜 예 → 좋은 예 |
| --- | --- |
| 소제목은 독자의 상황을 말로 쓴다. 상태 이름·개수 나열은 소제목이 아니다 | `화면 상태 4가지` → `데이터가 없을 때`, `오류가 났을 때` |
| 묶음 소제목은 범위가 보이게 쓴다 | `함께 정한 규칙` → `그 밖에 같이 정한 것`, `팝업이나 다운로드에서 실패했을 때` |
| 한 표에는 같은 층위만 넣는다. 성격이 다른 항목은 표를 나눈다 | 빈 상태 3종 + 오류 1종을 한 표에 ✗ → 표 둘 |
| 분류를 제시하면 나누는 기준을 표 앞 한 문장으로 먼저 쓴다 | `서버가 대답을 했는지로 둘을 나눕니다.` |
| 분류 표에는 `여기에 들어가는 것` 열을 두고 포함 항목을 나열한다 | 공통 오류: 500번대·404·나머지 400번대 / 네트워크 오류: 오프라인·연결 실패·시간 초과 |
| 적용되지 않는 예외는 본문에 섞지 않고 작은 표로 뺀다 | `오류 화면으로 가지 않는 것 2가지` 표 |
| 설명 문서·이슈의 순서: 무엇을 했나 → 상황별 표 → 예외 → 정해야 할 것 → 문서 링크 | 2026-09-16 safe-qr-docs 이슈 #15 초안 |

## 7. 윤문 모드 절차

사용자가 기존 글을 다듬어 달라고 하면 이 순서를 지킨다.

0. **먼저 이 글이 이미 사람 글로 읽히는지 본다.** §3 표현 몇 개만 걸리면 그 문장만 고치고 끝낸다. **고칠 이유가 없는 문장은 건드리지 않는다.** 좋은 원문에 손을 대면 대개 나빠진다(2026-09-16 디자인 시스템 개요 사례: 원문이 더 나았다).
1. **진단.** AI가 쓴 것처럼 보이는 이유를 있는 만큼만 짚는다(최대 5개, 없으면 "고칠 것 없음"). 각 이유에 원문 문장을 짧게 인용한다. 진단 개수를 채우려고 억지 항목을 만들지 않는다.
2. **고친다.** §1~§4, §6 적용. 최소 수정. 사실·숫자·고유명사·주장·인용·문단 순서·소제목 구조는 바꾸지 않는다. 바꾼 문장마다 "원문보다 나은가"를 묻고, 아니면 되돌린다.
3. **변경량 제한.** 목표는 원문의 15% 이하. 30%를 넘으면 멈추고 어디까지 고쳤는지 알린다. 더 고칠지 사용자가 정한다.
4. **보고.** 고친 글 → `주요 수정 포인트`(있는 만큼) → 남은 `[확인 필요]` 목록.

## 8. 마무리 자기 점검

글을 다 쓴 뒤 한 번 더 읽고 아래를 확인한다. 걸리는 곳은 고친다.

- [ ] 문단 첫 문장이 그 문단의 결론인가
- [ ] 대조·원인을 쪼개 `하지만/그래서`로 시작한 짧은 문장이 있는가. 반대로 생각이 둘인 60자 넘는 문장이 있는가
- [ ] `그 결과`, `이러한`, `이를 통해` 없이도 읽히는데 남겨 뒀는가
- [ ] §3 표의 표현이 하나라도 남았는가
- [ ] 한자어 명사구를 `~ㄴ지` 절이나 일상 동사로 풀 수 있는 곳이 있는가
- [ ] 내가 넣은 억지 동의어, 서술 중 괄호 설명, 조각 문장, 도치가 있는가. 있으면 원문으로 되돌린다
- [ ] `~습니다`가 세 번 연속인 곳이 있는가
- [ ] 비교 가능한 항목이 줄글로 늘어져 있는가
- [ ] 소제목이 상태 이름·개수 나열인가. 한 표에 층위가 다른 항목이 섞였는가(§6-1)
- [ ] 윤문이라면: 바꾼 문장이 원문보다 정말 나은가

## 참고 출처

이 규칙은 아래 자료의 패턴 목록과 전/후 예시를 한국어 기준으로 합치고 다듬은 것이다. 원문 예시 일부를 `references/ai-patterns-ko.md`에 인용했다.

- [epoko77-ai/im-not-ai](https://github.com/epoko77-ai/im-not-ai) — 한글 AI 티 제거기. 10대 분류(번역투·구조·관용구·리듬·수식·완곡·접속사·형식명사·장식)와 "의미 보존·30% 초과 시 중단" 원칙
- [woonjangahn — 한국어 AI 글쓰기에서 피해야 할 상투적 패턴](https://gist.github.com/woonjangahn/3ad4d8fe1804aed2e7cafc9493ec566f) — 어휘·문장 구조·한국어 특유 패턴 목록
- [blader/humanizer](https://github.com/blader/humanizer) — 영어 AI 글 패턴 25종과 전/후 예시. 위키백과 목록을 스킬로 옮긴 원형
- [Wikipedia: Signs of AI writing](https://en.wikipedia.org/wiki/Wikipedia:Signs_of_AI_writing) — 위키백과 AI 클린업 팀의 원전 목록
- [폭스씨지 — 챗GPT 블로그 글쓰기, AI 티 안 나게 쓰는 현실적인 방법](https://foxcg.com/chatgpt-blog-writing) — 페르소나·금지어·단점 강제 프롬프트, 번역투 교정 예시
- [요즘것들 — AI 과제 말투, 자연스럽게 바꾸는 법](https://www.allforyoung.com/users/@%EC%9A%94%EC%A6%98%EA%B2%83%EB%93%A4/ai-%EA%B3%BC%EC%A0%9C-%EB%A7%90%ED%88%AC-%EC%9E%90%EC%97%B0%EC%8A%A4%EB%9F%BD%EA%B2%8C-%EB%B0%94%EA%BE%B8%EB%8A%94-%EB%B2%95-%ED%94%84%EB%A1%AC%ED%94%84%ED%8A%B8-%ED%8F%AC%ED%95%A8-48d4c8b896ce) — "진단 먼저", 추상 표현→구체 상황, 작성자 해석 추가 프롬프트와 전/후 예시
- [브런치 maven — AI 특유의 뻔한 말투 지우는 프롬프트](https://brunch.co.kr/@maven/527) — 주어 생략·도치·문장 길이 변주 3조건
- [Promptway — The De-Slop Prompt Stack](https://promptway.com/blog/the-de-slop-prompt-stack) — 문장 길이 규칙, 구체성 패스(`[NEED SPECIFIC]`), 반론 삽입
- [Sid Saladi — AI Humanizer 101](https://sidsaladi.substack.com/p/ai-humanizer-101-50-prompts-to-make) — 내 글 샘플로 문체 복제, 관점 넣기
- [서울대저널 — 직접 쓴 글인데 AI 표절률 44%](https://www.snujn.com/news/articleView.html?idxno=75387) — 탐지기 기준(단어 반복·일정한 문장 길이·접속사·대조 구문)과 오판 사례
